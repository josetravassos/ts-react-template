// import { createSlice, createAsyncThunk, createEntityAdapter, EntityState } from '@reduxjs/toolkit'

// export const fetchFullProfile = createAsyncThunk(FETCH_FULL_PROFILE, async (studentId: number) => {
//     const response: any = await API.get(`${CAMS_ENDPOINT}/studentprofile/${studentId}/glance`);
//     return {...response.data, studentId};
//   });




//   function resolveAfter2Seconds(x) {
//     return new Promise(resolve => {
//       setTimeout(() => {
//         resolve(x);
//       }, 2000);
//     });
//   }
  
//   async function f1() {
//     var x = await resolveAfter2Seconds(10);
//     console.log(x); // 10
//   }
//   f1();


export default () => {
    
}